# Basic App model to clone, with Login and Register.

This project is to clone when you need a simple Login and Register App.
Simple CSS.
Without backend logic (You have to add it).

React JS, Axios, Chakra UI.
